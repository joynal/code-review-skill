interface:
  display_name: "Code Review"
  short_description: "Review code for evidence-backed, actionable defects"
  default_prompt: "Use $code-review to review these changes for actionable defects and report validation limits."
